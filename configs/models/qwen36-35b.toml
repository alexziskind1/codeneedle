# Qwen3.6 35B A3B — reasoning-on-by-default, ignores `/no_think` and every
# API thinking-disable knob we've tested. Need a generous max_tokens so the
# chain-of-thought has room AND the actual ~20-line answer still fits.

name              = "qwen3.6-35b-a3b"
base_url          = "http://localhost:1234"
temperature       = 0.0
max_tokens        = 6000
timeout           = 600.0
suppress_thinking = true   # appended anyway; harmless when ignored
