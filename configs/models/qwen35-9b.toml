# Qwen3 4B (small, non-reasoning by default).
# Honors the `/no_think` directive, so 1500 max_tokens is plenty.

name              = "qwen/qwen3.5-9b"
base_url          = "http://localhost:1234"
temperature       = 0.0
max_tokens        = 1500
timeout           = 600.0
suppress_thinking = true
